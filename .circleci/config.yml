version: 2.1
setup: true
orbs: 
  path-filtering: circleci/path-filtering@0.1.3

workflows:
  setup:
    jobs:
      - path-filtering/filter:
          mapping: |
            patha/.* run-path-a true
            pathb/.* run-path-b true
          config-path: .circleci/continue-config.yml
          