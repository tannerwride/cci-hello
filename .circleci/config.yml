version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - run: echo $DEMO
workflows:
  main:
    jobs:
     - build:
         context: production
         
          
     
