version: 2.1
executors:
  my-executor:
    docker:
      - image: cimg/base:stable
jobs: 
  build-a:
    executor: my-executor
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: false
      - run: 
          name: Build A
          command: echo $ORG_ID
  
workflows:
  workflow:
    jobs:
      - build-a:
          context: cli-context
     
