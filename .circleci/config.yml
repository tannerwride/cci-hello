version: 2.1

jobs: 
  build-a:
    executor: tannerwride/dockerrunner
    steps:
      - checkout
      - run: 
          name: Build A
          command: echo "hello"

  
workflows:
  workflow:
    jobs:
      - build-a
     
