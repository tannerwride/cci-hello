version: 2.1

jobs: 
  build-a:
    machine: true
    resource_class: tannerwride/dockerrunner
    steps:
      - checkout
      - run: 
          name: Build A
          command: echo "hello from a runner!"

  
workflows:
  workflow:
    jobs:
      - build-a
     
