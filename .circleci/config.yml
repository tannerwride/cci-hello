version: 2.1
jobs: 
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: |
          if [[ $(cat hello.txt) == "How do you do?" ]]; then
            echo "Passed!";
          else
            echo "Wrong text!"; exit 1
          fi
workflows:
  main-workflow:
    jobs:
      - build 
          