version: 2.1
executors:
  my-executor:
    docker:
      - image: cimg/base:stable
jobs: 
  build-a:
    executor: my-executor
    steps:
      - checkout
      - run: 
          name: Build A
          command: echo "hi"
# workflows  
workflows:
  workflow:
    jobs:
      - build-a
          
     
