version: 2.1
executors:
  basic:
    docker:
      - image: cimg/base:stable
    resource_class: small
jobs: 
  build-a:
    executor: basic
    steps:
      - checkout
      - run: 
          name: Build A
          command: echo "hello"

  test-a:
    executor: basic
    steps:
      - checkout
      - run:
          name: Test A
          command: echo "this is a test job"
workflows:
  main-workflow:
    jobs:
      - build-a
      - test-a
          