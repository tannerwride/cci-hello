version: 2.1
executors:
  my-executor:
    docker:
      - image: cimg/base:stable
jobs: 
  build-a:
    executor: my-executor
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run: 
          name: Build A
          command: echo "hello"
  
workflows:
  workflow:
    jobs:
      - build-a
     
