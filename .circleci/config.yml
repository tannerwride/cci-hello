version: 2.1

jobs: 
  build-a:
    executor: tannerwride/runtannerrun
    steps:
      - checkout
      - run: 
          name: Build A
          command: echo "hello"

  
workflows:
  workflow:]
    jobs:
      - build-a
     
